class PantallaPerdiste {
  mostrar() {
    background(0); // Fondo negro
    textSize(32);
    fill(255);
    textAlign(CENTER);
    text("¡Perdiste! Haz clic en el botón para volver a jugar.", width / 2, height / 2);
  }
}
