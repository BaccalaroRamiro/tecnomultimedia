class PantallaInicio {
  mostrar() {
    background(0); // Fondo negro
    textSize(32);
    fill(255);
    textAlign(CENTER);
    text("¡Haz clic en el botón para comenzar!", width / 2, height / 2);
  }
}
